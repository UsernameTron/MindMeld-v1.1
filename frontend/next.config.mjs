/** @type {import('next').NextConfig} */
const nextConfig = {
  // Ensure Pages Router is used
  reactStrictMode: true,
  // No need to specify appDir: false in Next.js 15.x as it defaults to Pages Router
};

export default nextConfig;
