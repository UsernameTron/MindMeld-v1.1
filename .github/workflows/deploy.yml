# Minimal deploy workflow (temporary placeholder)
# See .github/TEMP_WORKFLOWS_README.md for details

name: Deploy

on:
  push:
    branches: [main, production]
  pull_request:
    branches: [main, production]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm ci
      - name: Build
        run: npm run build || true
      - name: Test
        run: npm test || true
      - name: Deploy (placeholder)
        run: echo "Deployment step placeholder. No real deploy logic."
